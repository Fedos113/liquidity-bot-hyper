services:
  dashboard:
    build:
      context: ..
      dockerfile: liqbot2/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./liqbot2/data:/app/liqbot2/data
      - ../.env:/app/.env
    restart: unless-stopped
